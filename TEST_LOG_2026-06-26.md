# Test Log 2026-06-26

Project: `AcondicionamientoAnalogico`.

## Hardware

- PSoC programmer: `KitProg (CMSIS-DAP/236111)`.
- ESP bridge/log port: `COM8`.
- PSoC PC UART on `COM6` is not usable in the current programming mode; all
  runtime commands/logs go through the ESP.

## Firmware Changes

- GEO calibration order is `PGA -> BP -> ADDER -> LP`.
- GEO differential target is `0 counts` for all stages.
- All GEO stage adelantos are near `1.5 V` (`1504 mV`, DAC code `94`) at boot
  and at the start of calibration.
- Gains used by the PI:
  - `GEO_PGA`: dynamic signed gain `1 - GainDirecta`.
  - `GEO_BP`: `+1`.
  - `GEO_ADDER`: `+3`.
  - `GEO_LP`: `+6`.
- `FIR_calibration` is loaded into the hardware `Filter` for calibration.
  `FIR_adquisition` is restored when calibration completes.
- The PI reads the hardware Filter output through `DMA_Filter_RAM`; no software
  averaging/refine pass is used.
- Added `CAL_PI_FIR_SETTLE_SAMPLES = 512` so the FIR can settle after AMux
  switching and filter-history reset before the PI starts moving the DAC.
- Increased `CAL_PI_TIMEOUT_SAMPLES` to `30000` per stage, prioritizing a
  working calibration over speed.
- Removed early abort at DAC rails. A stage now gets the full timeout to let
  the low-cutoff FIR show the effect of the last DAC movement.
- Disabled diagnostic DAC sweeps during normal calibration; they were useful to
  prove authority but too intrusive for the real run.
- If a stage still fails at a rail, the firmware restores that VDAC to the
  stage center/adelanto instead of leaving it saturated.
- `AMux_ADC` selects exactly one signal channel plus the capacitor channel
  (`AMux=4`) during calibration, then disconnects the capacitor and restores
  the capture path at the end.
- Button-triggered calibration remains enabled.
- EEPROM calibration loading on boot is disabled for this bring-up path
  (`PSOC_LOAD_NV_CAL_ON_BOOT=0`) so old saved DAC values do not override the
  adelantos.

## Build And Program

Build command:

```powershell
Push-Location "C:\Github\Tesis\src\psoc\AcondicionamientoAnalogico.cydsn"
try {
  & "C:\Program Files (x86)\Cypress\PSoC Creator\4.4\PSoC Creator\bin\cyprjmgr.exe" `
    -wrk "AcondicionamientoAnalogico.cywrk" `
    -build
} finally {
  Pop-Location
}
```

Final build result:

- Build succeeded at `2026-06-26 19:25:48`.
- Flash used: `27398/262144 bytes`.
- SRAM used: `49680/65536 bytes`.
- Warning only: `cal_servo_clip_integral` defined but not used.

Program result:

- PPCLI port: `KitProg (CMSIS-DAP/236111)`.
- Rows programmed/verified: `0..109`.
- Final successful program log:
  `C:\Users\elias\AppData\Local\Temp\psoc_program_acondicionamiento_stable2_20260626_192601.log`.

## Runtime Through ESP

Captured log before the final timeout/no-early-rail-abort tweak:

`C:\Users\elias\AppData\Local\Temp\esp_psoc_geo_stable_20260626_192308.log`

Important results from that log:

- `GEO_PGA`: `ok=0` only because the then-current `5 s` timeout cut it early;
  measured around `-223 mV`, DAC restored to `127`.
- `GEO_BP`: did not show useful authority from `VDAC_ref_BP`; it remained near
  `+1.04 V` and DAC restored to the adelanto (`94`). The code does write
  `VDAC_ref_BP_SetValue`, so this looks like hardware/routing/authority to
  inspect later, not a missing firmware write.
- `GEO_ADDER`: `ok=1`, `dac=128`, about `-37 mV`.
- `GEO_LP`: `ok=1`, `dac=145`, about `-85 mV`.

After the final firmware program, the board was observed on the bench as
approximately calibrated and functional. LP was still visibly less centered
than desired, but its `~85-107 mV` residual is inside the current physical
deadband for gain `6`: one VDAC LSB is `16 mV`, so `6 * 16 mV * 1.2` is about
`115 mV`.

## Next Tuning Notes

- Do not re-enable diagnostic sweeps for normal use; use them only in a
  deliberate authority test.
- LP can be made tighter later by tuning PI/deadband, but the present value is
  consistent with the requested gain-based deadband.
- BP should be checked electrically because the measured BP channel stayed near
  `+1.04 V` while the BP VDAC command changed/restored normally.
